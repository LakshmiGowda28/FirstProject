trigger:
- main

variables:
  DB_PASSWORD: 'Test@123'  # or use a secure variable group instead

pool:
  vmImage: 'ubuntu-latest'

steps:
- task: Maven@3
  inputs:
    mavenPomFile: 'pom.xml'
    goals: 'package'

- script: echo "Simulated deployment with secret: $(DB_PASSWORD)"
  displayName: 'Simulate Deployment Step'